var dfg = '../lib';

describe("Electricity", function() {
  it("calls my tests", function() {
    expect(true).toBe(true);
  });
  it("Loads dfg", function() {
    expect(dfg).toBeTruthy();
  });
});
